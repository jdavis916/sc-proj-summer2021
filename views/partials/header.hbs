<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>{{title}}</title>
    <!-- <link rel='icon' href='/img/favicon.ico' type='image/x-icon'/> -->
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <!--Font awesome icons-->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"/>
    <!--selected fonts-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400&family=Montserrat:wght@300;600&display=swap" rel="stylesheet">
    <!--Custom CSS-->
    <link rel="stylesheet" href="/css/style.css" />
    <script>
        var loggedIn = {{loggedIn}};
        console.log(loggedIn);
        var profPic = '{{profPic}}';
    </script>
  </head>
  <body>
      <header>
        <nav class="navbar navbar-expand-md navbar-dark navBar">
          <div class="container">
            <a href="/" id="logoBox"><img class="navbar-brand logo" src="/img/autono_1.png" alt="autono logo" id="logo"></a>
            <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse" id="navbarCollapse" style="">
              <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                  <a class="nav-link  navLink {{#if active.home}}active{{/if}}" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link navLink {{#if active.rental}}active{{/if}}" href="/rental">Rental</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link  navLink {{#if active.contact}}active{{/if}}" href="/contact">Contact</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link navLink {{#if active.settings}}active{{/if}}" href="/settings">Settings</a>
                </li>
              </ul>
              <ul class="navbar-nav mb-2 mb-md-0">
                <li class="nav-item">
                  <a class="nav-link navLink" id="btnLogout" href='#'>{{#if loggedIn}}Sign out{{else}}Login{{/if}}</a>
                </li>
                <li class="nav-item">
                  {{#if loggedIn}}
                  <a class="nav-link navLink" id="btnProf"  href='#'></a>
                  {{else}}
                  <a class="nav-link navLink" id="btnProf"  href='/signup'>Sign Up</a>
                  {{/if}}
                </li>
                <li class="nav-item">
                  {{#if loggedIn}}
                  <a class="nav-link navLink {{#if active.profile}}active{{/if}}" id="btnProf"  href='/profile'>{{who}}</a>
                  {{else}}
                  <a class="nav-link navLink" id="btnProf"  href='#'></a>
                  {{/if}}
                </li> 
              </ul>
            </div>
          </div>
        </nav>
      </header>